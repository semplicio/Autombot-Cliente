name: Inspect OpenVPN native binary
on:
  workflow_dispatch:
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: file app/src/main/jniLibs/arm64-v8a/libopenvpn.so
